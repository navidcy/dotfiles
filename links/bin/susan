#!/bin/bash
msg="$@"
if [ $# -eq 0 ]; then
    msg=`cat`
fi
to="Susan Louise Damsgaard Sonnesen"
#to="Anders Damsgaard"

echo "$msg"
osascript -e 'tell application "Messages" to send "'"$msg"'" to buddy "'"$to"'"'
