#!/bin/bash
set -e
remotecommand='tmux attach -dt 0'
host=idkfa.ucsd.edu
user=ad
if [ "$1" = "-s" ]; then
    ssh $user@$host -t $remotecommand
elif [ "$1" = "-a" ]; then
    autossh $user@$host
else
    mosh $user@$host -- $remotecommand
fi
